#ifndef FILE_HANDLING_H
#define FILE_HANDLING_H

// takes a file name and checks extension to do what is needed and does it 
void mnu_filesystem_handle_file(const char* file_name, char* path_main);

#endif